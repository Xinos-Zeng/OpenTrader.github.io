# OpenTrader - 量化交易可视化平台

## 项目概述

OpenTrader 是 AgentTrader 自进化量化交易框架的 Web 前端，提供简约清新的可视化界面，让用户能够直观地体验 AI Agent 驱动的量化交易策略优化过程。

对应的后端项目为AgentTrader

## 设计原则

1. **简约清新**：界面保持简洁，信息层次清晰，避免视觉干扰
2. **移动优先**：响应式设计，兼顾移动端体验
3. **卡片化布局**：核心信息以卡片形式呈现，便于浏览和交互
4. **增量开发**：每个阶段交付可运行的完整功能
5. **代码精简**：不过度设计，轻量化优先，能快速跑起来

---

## 技术栈

| 分类 | 技术 | 说明 |
|------|------|------|
| 框架 | React 18 | Hooks + TypeScript |
| 构建 | Create React App | react-scripts，零配置 |
| 样式 | 原生 CSS | CSS 变量 + 自定义类 |
| 路由 | React Router | 单页应用路由 |
| 状态 | Zustand | 轻量级状态管理 |
| 请求 | Axios | HTTP 客户端 |
| 图表 | Recharts | 数据可视化 |

---

## 项目结构

```
src/
├── api/              # API 请求封装
│   ├── client.ts     # Axios 实例配置
│   ├── auth.ts       # 认证相关 API
│   ├── strategy.ts   # 策略相关 API
│   └── trade.ts      # 交易相关 API
├── components/       # 通用组件
│   ├── NavBar.tsx    # 导航栏
│   ├── StrategyCard.tsx
│   └── StatsCard.tsx
├── pages/            # 页面组件
│   ├── Login.tsx
│   ├── Register.tsx
│   ├── Dashboard.tsx
│   └── backtest/
├── stores/           # Zustand 状态
│   ├── authStore.ts
│   └── strategyStore.ts
├── types/            # TypeScript 类型
├── index.tsx         # 入口
├── App.tsx           # 路由配置
└── index.css         # 全局样式
```

---

## 运行方式

```bash
# 安装依赖
npm install

# 启动开发服务器
npm start

# 构建生产版本
npm run build
```

---

## 开发阶段

### 阶段一~三：基础功能 ✅ 已完成

- 用户登录/注册
- Dashboard 策略选择
- 回测配置和结果展示

---

### 阶段四：流式回测与收藏功能 ✅ 已完成

**目标**：实现回测过程的实时流式展示，支持收藏回测结果

**功能需求**：
1. 回测发起后实时显示每笔交易信号（消息流）
2. 每用户同时只能进行一个回测
3. 离开页面或手动取消可终止回测
4. 回测完成后可保存到收藏
5. 收藏列表页面展示历史回测
6. 收藏详情页显示完整信息

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `pages/backtest/BacktestStream.tsx` | 流式回测页面 |
| `pages/Favorites.tsx` | 收藏列表页 |
| `pages/FavoriteDetail.tsx` | 收藏详情页 |
| `stores/backtestStore.ts` | 回测状态管理 |
| `api/favorites.ts` | 收藏 API |

---

### 阶段五：参数调整与策略保存 🚧 进行中

**目标**：允许用户调整策略参数并保存为自定义策略，Dashboard 展示用户保存的策略

**功能需求**：
1. 用户可以修改策略参数后保存为自己的策略
2. Dashboard 分为两区：系统策略 + 我的策略
3. 我的策略支持删除操作
4. 用户保存的策略可直接用于回测

**技术方案**：
- 后端新增用户策略表 `user_strategies`
- 后端新增策略 CRUD API
- 前端 Dashboard 分区展示
- 前端回测页面增加"保存策略"功能

**API 设计**：
```
POST   /api/strategies       # 保存策略
GET    /api/strategies       # 获取用户策略列表
DELETE /api/strategies/{id}  # 删除策略
```

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `pages/Dashboard.tsx` | 新增我的策略区域 |
| `components/Modal.tsx` | 保存策略弹窗 |
| `api/strategy.ts` | 策略 CRUD API |

**验收标准**：
- [ ] Dashboard 下方显示"我的策略"区域
- [ ] 用户可以保存自定义策略（自定义名称和参数）
- [ ] 我的策略卡片支持删除
- [ ] 用户策略可以直接选择用于回测

---

### 阶段六：Agent 交互 ✅ 已完成

**目标**：在回测过程中引入 AI Agent 量化助手，实现自动策略优化

**功能需求**：
1. 回测界面新增 Agent 配置选项：
   - 勾选框：启用 Agent 量化助手
   - 输入框：检测周期（每 X 天 Agent 介入分析）
2. 启用 Agent 后的回测流程：
   - 每到达检测周期，暂停回测
   - Agent 分析近期交易数据，判断是否需要调整策略参数
   - 如需调整，应用新参数后继续回测
3. Agent 消息卡片展示：
   - 在消息流中显示 Agent 的分析过程和建议
   - 显示参数调整内容（如有）

**技术方案**：
- 后端 `run_stream` 方法新增 Agent 周期检测逻辑
- 后端调用 `TradingAgent.analyze_and_optimize()` 方法
- SSE 新增 `agent` 类型消息推送 Agent 分析结果
- 前端新增 Agent 消息卡片样式

**API 设计**：
```
# 回测 SSE 端点新增参数
GET /api/backtest/stream
  ?agent_enabled=true       # 启用 Agent
  &agent_interval=30        # 检测周期（天）
```

**SSE 消息类型新增**：
```json
{"type": "agent", "data": {
  "action": "analyzing",    // analyzing | adjusted | no_change
  "message": "正在分析近30天交易数据...",
  "params_before": {...},   // 调整前参数（如有调整）
  "params_after": {...}     // 调整后参数（如有调整）
}}
```

**验收标准**：
- [ ] 回测界面可勾选启用 Agent
- [ ] 可设置 Agent 检测周期
- [ ] 回测过程中 Agent 定期介入分析
- [ ] Agent 分析和调整过程以消息卡片展示
- [ ] Agent 调整参数后回测使用新参数继续

---

### 阶段七：收益率排行榜 ✅ 已完成

**目标**：在首页展示收益率排行榜，显示 TOP 10 用户

**功能需求**：
1. Dashboard 新增排行榜卡片
2. 显示前 10 名收益率最高的用户
3. 区分真实用户和模拟用户（模拟用户带特殊标识）
4. 显示当前用户的排名（如果有保存的策略）
5. 点击排行榜项可查看策略详情（如果是真实用户）

**UI 设计**：
```
┌─────────────────────────────────────┐
│  🏆 收益率排行榜                     │
├─────────────────────────────────────┤
│  🥇 1. 量化小王子        +156.8%    │  ← 模拟用户
│  🥈 2. trader_001        +145.2%    │  ← 真实用户
│  🥉 3. 不追涨杀跌        +128.5%    │  ← 模拟用户
│     4. 佛系交易员        +115.2%    │
│     5. 代码搬运工         +98.7%    │
│     ...                             │
├─────────────────────────────────────┤
│  👤 你的最佳: 第 15 名    +45.6%    │  ← 当前用户
└─────────────────────────────────────┘
```

**技术方案**：
- 新增 `api/leaderboard.ts` - 排行榜 API
- 新增 `components/LeaderboardCard.tsx` - 排行榜组件
- Dashboard 集成排行榜卡片
- 排行榜数据缓存，避免频繁请求

**API 调用**：
```typescript
// GET /api/leaderboard
interface LeaderboardItem {
  rank: number;
  username: string;
  avatar: string | null;
  return_rate: number;
  strategy_name: string | null;
  is_mock: boolean;
}
```

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `api/leaderboard.ts` | 排行榜 API |
| `components/LeaderboardCard.tsx` | 排行榜卡片组件 |
| `components/LeaderboardCard.css` | 排行榜样式 |

**验收标准**：
- [ ] Dashboard 显示排行榜卡片
- [ ] 排行榜显示 TOP 10 用户
- [ ] 模拟用户和真实用户有视觉区分
- [ ] 显示当前用户的最佳排名
- [ ] 排行榜样式美观，动画流畅

---

### 阶段八：策略预览与管理增强 ✅ 已完成

**目标**：配合后端策略字符串化重构，增强策略预览和管理功能

**背景**：
- 后端阶段十四将策略从代码文件改为字符串存储
- 策略现在可以被 Agent 完全修改，不仅仅是参数
- 前端需要支持策略预览、代码查看等功能

**功能需求**：
1. 策略卡片新增"预览"功能，显示策略简述
2. 点击卡片可查看策略详情（描述 + 代码高亮）
3. "我的策略"支持编辑策略名称和描述
4. 策略列表新增"系统策略"标签区分

**UI 设计**：

#### 策略卡片
```
┌────────────────────────────────────┐
│  📈 双均线交叉策略                  │
│  ─────────────────────────────────  │
│  经典双均线策略，金叉买入死叉卖出    │
│                                     │
│  快线: 5  慢线: 20                  │
│                                     │
│  [查看详情]  [开始回测]             │
└────────────────────────────────────┘
```

#### 策略详情弹窗
```
┌─────────────────────────────────────────────┐
│  双均线交叉策略                    [×]      │
├─────────────────────────────────────────────┤
│  📝 策略描述                                │
│  经典双均线交叉策略，使用快线和慢线的交叉  │
│  作为买卖信号...                            │
│                                             │
│  💻 策略代码                                │
│  ┌─────────────────────────────────────────┐│
│  │ FAST_PERIOD = 5                         ││
│  │ SLOW_PERIOD = 20                        ││
│  │                                         ││
│  │ def on_bar(df):                         ││
│  │     ...                                 ││
│  └─────────────────────────────────────────┘│
│                                             │
│  [开始回测]  [保存到我的策略]               │
└─────────────────────────────────────────────┘
```

**技术方案**：
- 集成 `react-syntax-highlighter` 实现代码高亮
- 新增 `StrategyDetailModal` 组件
- 策略卡片增加预览信息

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `components/StrategyCard.tsx` | 更新：增加预览信息 |
| `components/StrategyDetailModal.tsx` | 新增：策略详情弹窗 |
| `components/CodeBlock.tsx` | 新增：代码高亮组件 |
| `api/strategy.ts` | 更新：获取策略代码 |

**验收标准**：
- [x] 策略卡片显示策略简述
- [x] 点击可查看策略详情和代码
- [x] 代码显示有语法高亮
- [x] 系统策略和用户策略有视觉区分

**交付物**：
- `components/CodeBlock.tsx` ✅ - 代码高亮组件
- `components/StrategyDetailModal.tsx` ✅ - 策略详情弹窗
- `api/strategy.ts` ✅ - 预置策略 API

---

### 阶段九：Agent 对话界面 ✅ 已完成

**目标**：实现 Agent 对话窗口，用户可通过自然语言生成/修改策略

**背景**：
- 后端阶段十五将实现策略生成 Agent 服务
- 使用 [pi-mono](https://github.com/badlogic/pi-mono) 的 `@mariozechner/pi-web-ui` 组件
- 用户可以通过对话方式创建复杂的量化策略

**用户场景**：
1. 用户点击导航栏的"AI 助手"按钮
2. 打开 Agent 对话窗口（侧边栏或独立页面）
3. 用户输入："帮我写一个 MACD 策略"
4. Agent 返回策略描述和代码预览
5. 用户点击"保存到我的策略"
6. 用户可以继续对话修改策略

**UI 设计**：

#### 方案 A：侧边栏对话（推荐）
```
┌─────────────────────────────────────────────────────────────────┐
│  🏠 Dashboard              [🤖 AI助手]  [用户]                   │
├───────────────────────────────────────────┬─────────────────────┤
│                                           │ 💬 AI 量化助手       │
│     系统策略                              │─────────────────────│
│     ┌────┐  ┌────┐  ┌────┐               │ 🤖 你好！我可以帮你:  │
│     │ MA │  │RSI │  │MACD│               │    • 生成新策略       │
│     └────┘  └────┘  └────┘               │    • 修改已有策略     │
│                                           │    • 解释策略逻辑     │
│     我的策略                              │                     │
│     ┌────┐  ┌────┐                        │ 👤 帮我写一个MACD策略 │
│     │自定│  │自定│                        │                     │
│     └────┘  └────┘                        │ 🤖 好的，这是MACD策略 │
│                                           │    ┌───────────────┐ │
│                                           │    │ 策略预览卡片  │ │
│                                           │    │ [保存] [回测] │ │
│                                           │    └───────────────┘ │
│                                           │─────────────────────│
│                                           │ [输入消息...]  [发送]│
└───────────────────────────────────────────┴─────────────────────┘
```

#### 方案 B：独立对话页面
```
/agent 路由
┌─────────────────────────────────────────────────────────────────┐
│  🏠  📊  🤖  ⭐  👤                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    💬 AI 量化助手                               │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                                                             ││
│  │  🤖  你好！我是你的 AI 量化助手，我可以帮你：                ││
│  │      • 根据你的想法生成交易策略                             ││
│  │      • 修改和优化已有策略                                   ││
│  │      • 解释策略代码和逻辑                                   ││
│  │                                                             ││
│  │  👤  帮我写一个 MACD 策略，金叉买入死叉卖出                  ││
│  │                                                             ││
│  │  🤖  好的！我为你生成了一个 MACD 交叉策略：                  ││
│  │                                                             ││
│  │      ┌───────────────────────────────────────┐              ││
│  │      │ 📈 MACD 交叉策略                       │              ││
│  │      │ MACD 金叉买入，死叉卖出...            │              ││
│  │      │                                       │              ││
│  │      │ [查看代码]  [保存策略]  [开始回测]    │              ││
│  │      └───────────────────────────────────────┘              ││
│  │                                                             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌───────────────────────────────────────────────┐  [发送 ➤]   │
│  │ 输入你的策略想法...                           │              │
│  └───────────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

**技术方案**：
- 考虑集成 pi-mono 的 `@mariozechner/pi-web-ui` 对话组件
- 如不适用，自行实现轻量对话组件
- 对话消息支持渲染策略卡片
- 策略卡片内嵌保存/回测操作

**消息类型**：
```typescript
interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  strategy?: {              // 如果消息包含策略
    name: string;
    code: string;
    description: string;
  };
  timestamp: Date;
}
```

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `pages/Agent.tsx` | Agent 对话页面 |
| `components/ChatWindow.tsx` | 对话窗口组件 |
| `components/ChatMessage.tsx` | 消息气泡组件 |
| `components/StrategyPreviewCard.tsx` | 策略预览卡片（对话内） |
| `stores/chatStore.ts` | 对话状态管理 |
| `api/agent.ts` | Agent 对话 API |

**验收标准**：
- [x] 导航栏有 AI 助手入口
- [x] 对话界面美观，交互流畅
- [x] 用户可以通过自然语言描述策略需求
- [x] Agent 返回的策略可以预览
- [x] 策略可以一键保存到"我的策略"
- [x] 策略可以直接发起回测
- [x] 支持多轮对话修改策略

**交付物**：
- `pages/Agent.tsx` ✅ - Agent 对话页面
- `components/ChatMessage.tsx` ✅ - 消息气泡组件
- `components/StrategyPreviewCard.tsx` ✅ - 策略预览卡片
- `stores/chatStore.ts` ✅ - 对话状态管理
- `api/agent.ts` ✅ - Agent 对话 API
- `components/NavBar.tsx` ✅ - 新增 AI 助手入口
- `App.tsx` ✅ - 新增 /agent 路由

---

### 阶段十：期货资讯分析界面 ⏳ 待开始

**目标**：在 Agent 对话中支持期货资讯和行情分析

**背景**：
- 后端阶段十六将实现期货资讯分析 Agent
- 用户可以询问行情走势、相关新闻等

**用户场景**：
1. 用户在 Agent 对话中询问："最近螺纹钢走势如何？"
2. Agent 返回行情分析和趋势图
3. 用户询问："有什么相关新闻？"
4. Agent 返回新闻摘要列表

**UI 设计**：

#### 行情分析卡片
```
┌───────────────────────────────────────────┐
│ 📊 螺纹钢 (SHFE.rb2505) 行情分析          │
├───────────────────────────────────────────┤
│                                           │
│  [====== 价格走势图 ======]               │
│                                           │
│  当前价格: 3856  ▲ +1.2%                  │
│  5日均线: 3820   10日均线: 3795           │
│                                           │
│  📈 趋势: 短期看涨                         │
│  建议: 关注 3900 压力位                    │
└───────────────────────────────────────────┘
```

#### 新闻摘要卡片
```
┌───────────────────────────────────────────┐
│ 📰 螺纹钢相关新闻                         │
├───────────────────────────────────────────┤
│                                           │
│ • 钢材市场周报：需求回暖，价格震荡上行    │
│   2026-03-03  来源: 期货日报              │
│                                           │
│ • 铁矿石供应紧张，成本支撑钢价           │
│   2026-03-02  来源: 中国钢铁新闻网        │
│                                           │
│ • 基建投资加速，黑色系期货集体上涨        │
│   2026-03-01  来源: 财联社                │
│                                           │
└───────────────────────────────────────────┘
```

**技术方案**：
- 对话消息支持渲染行情图表（使用 Recharts）
- 新闻卡片组件展示新闻摘要
- 复用 Agent 对话页面，扩展消息类型

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `components/MarketAnalysisCard.tsx` | 行情分析卡片 |
| `components/NewsCard.tsx` | 新闻摘要卡片 |
| `components/MiniChart.tsx` | 迷你行情图 |

**验收标准**：
- [ ] 用户可以询问品种行情
- [ ] Agent 返回行情分析和图表
- [ ] 用户可以询问相关新闻
- [ ] 新闻摘要展示清晰

---

### 阶段十一：模拟交易 ⏳ 待开始

实时模拟交易监控（类似流式回测，但持续运行），需要后端支持

---

### 阶段十四：ReAct Agent 推理过程展示 ⏳ 待开始

**目标**：配合后端 AgentScope ReAct Agent 升级，展示 Agent 的推理过程

**背景**：
- 后端阶段十八将 Agent 升级为 AgentScope ReAct 架构
- ReAct 模式包含 Thought（思考）→ Action（行动）→ Observation（观察）循环
- AgentScope 支持 Tool Groups 动态管理
- 前端需要展示推理过程和工具调用，提升用户体验和可解释性

**功能需求**：

1. **推理步骤展示**
   - 显示 Agent 的思考过程（Thought）
   - 显示正在调用的工具（Action）
   - 显示工具执行结果（Observation）
   - 循环步骤有清晰的视觉区分

2. **工具调用可视化**
   - 显示正在调用的工具名称
   - 显示工具参数
   - 显示执行状态（进行中/完成/失败/中断）
   - 显示返回结果摘要

3. **进度指示**
   - 多步骤任务显示当前步骤
   - 支持折叠/展开详细推理过程
   - 最终答案突出显示

4. **Agent 切换提示**（多 Agent 协作时）
   - 显示 Agent 切换信息
   - 说明切换原因

**UI 设计**：

#### 推理过程消息卡片
```
┌───────────────────────────────────────────────┐
│ 🤖 AI 助手正在思考...                          │
├───────────────────────────────────────────────┤
│                                               │
│ 💭 Thought #1                                 │
│ ┌───────────────────────────────────────────┐ │
│ │ 用户想了解最近的交易表现，我需要先获取   │ │
│ │ 交易统计数据...                           │ │
│ └───────────────────────────────────────────┘ │
│                                               │
│ ⚡ Action: get_trade_stats                    │
│ ┌───────────────────────────────────────────┐ │
│ │ 参数: {"days": 30}                        │ │
│ │ 状态: ✅ 完成                              │ │
│ └───────────────────────────────────────────┘ │
│                                               │
│ 👁 Observation                                │
│ ┌───────────────────────────────────────────┐ │
│ │ 获取到 30 天交易数据：                    │ │
│ │ 总交易: 45 笔, 胜率: 62%, 盈亏: +15.3%   │ │
│ └───────────────────────────────────────────┘ │
│                                               │
│ 💭 Thought #2                                 │
│ ┌───────────────────────────────────────────┐ │
│ │ 数据显示表现不错，让我分析一下具体的     │ │
│ │ 盈亏分布...                               │ │
│ └───────────────────────────────────────────┘ │
│                                               │
│ ─────────────────────────────────────────────  │
│                                               │
│ 📊 最终答案                                   │
│ 根据分析，你最近 30 天的交易表现良好...      │
│                                               │
└───────────────────────────────────────────────┘
```

#### 折叠模式（默认）
```
┌───────────────────────────────────────────────┐
│ 🤖 AI 助手                                     │
├───────────────────────────────────────────────┤
│                                               │
│ 📊 最终答案                                   │
│ 根据分析，你最近 30 天的交易表现良好...      │
│                                               │
│ [📋 查看推理过程 (3步)]                       │
│                                               │
└───────────────────────────────────────────────┘
```

**技术方案**：

1. **SSE 事件处理**
   - 新增 `thought`、`action`、`observation`、`handoff` 事件类型
   - 实时更新推理步骤状态

2. **状态管理**
   ```typescript
   interface ReActStep {
     type: 'thought' | 'action' | 'observation' | 'handoff';
     content: string;
     step: number;             // 步骤序号
     toolName?: string;        // action 类型
     toolParams?: object;      // action 类型
     status?: 'pending' | 'running' | 'done' | 'error' | 'interrupted';
     fromAgent?: string;       // handoff 类型
     toAgent?: string;         // handoff 类型
   }

   interface ChatMessage {
     // ... 现有字段
     reactSteps?: ReActStep[];  // ReAct 推理步骤
     showReactSteps?: boolean;  // 是否展开显示
   }
   ```

3. **组件设计**
   ```
   ChatMessage
   ├── MessageContent (最终答案)
   ├── ReActStepsToggle (折叠/展开按钮)
   └── ReActStepsPanel (推理步骤面板)
       ├── ThoughtStep
       ├── ActionStep (含中断状态)
       ├── ObservationStep
       └── HandoffStep (Agent 切换)
   ```

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `components/ReActStepsPanel.tsx` | 推理步骤面板 |
| `components/ReActStepsPanel.css` | 推理步骤样式 |
| `components/ThoughtStep.tsx` | 思考步骤组件 |
| `components/ActionStep.tsx` | 行动步骤组件（含中断状态） |
| `components/ObservationStep.tsx` | 观察步骤组件 |
| `components/HandoffStep.tsx` | Agent 切换组件 |
| `stores/chatStore.ts` | 更新：支持 ReAct 步骤 |
| `api/agent.ts` | 更新：处理新 SSE 事件 |
| `pages/Agent.tsx` | 更新：渲染推理过程 |

**验收标准**：
- [ ] Agent 回复显示推理过程
- [ ] 支持折叠/展开推理步骤
- [ ] 工具调用有状态指示（含中断状态）
- [ ] 流式推理过程实时更新
- [ ] 最终答案突出显示

---

## 当前状态

**当前阶段**：核心功能已完成，准备进入 Agent 架构升级的前端适配

**进度**：阶段七、八、九、十二、十三已完成

**后续规划**：
| 阶段 | 名称 | 优先级 | 状态 |
|------|------|--------|------|
| 七 | 收益率排行榜 | 中 | ✅ 已完成 |
| 八 | 策略预览与管理增强 | 高 | ✅ 已完成 |
| 九 | Agent 对话界面 | 高 | ✅ 已完成 |
| 十 | 期货资讯分析界面 | 低 | 暂不开发 |
| 十一 | 模拟交易 | 中 | ⏳ 待开始 |
| 十二 | AI 对话流式输出 | 高 | ✅ 已完成 |
| 十三 | 策略查看与优化入口 | 高 | ✅ 已完成 |
| **十四** | **ReAct Agent 推理过程展示** | **高** | **⏳ 待开始** |

---

### 阶段十二：AI 对话流式输出 ✅ 已完成

**目标**：升级 AI 助手对话为流式输出，实时显示生成内容

**技术方案**：
- 使用 fetch + ReadableStream 接收 SSE 事件
- 实时更新消息内容
- 显示流式光标动画
- 提供停止按钮，可中断生成
- 超时机制：60 秒无新 token 则超时

**涉及文件**：
| 文件 | 说明 |
|------|------|
| `api/agent.ts` ✅ | 新增 `chatStrategyStream` 方法 |
| `stores/chatStore.ts` ✅ | 新增 `streamingContent`、`isStreaming`、`stopStreaming` |
| `pages/Agent.tsx` ✅ | 支持流式内容显示和停止按钮 |
| `components/ChatMessage.tsx` ✅ | 支持流式光标动画 |
| `components/ChatMessage.css` ✅ | 流式光标样式 |
| `pages/Agent.css` ✅ | 停止按钮样式 |

**验收标准**：
- [x] AI 回复实时显示，逐字输出
- [x] 显示流式光标动画
- [x] 提供停止按钮，可中断生成
- [x] 60 秒无新内容自动超时

---

### 阶段十三：策略查看与优化入口 ✅ 已完成

**目标**：增强"我的策略"和回测页面的策略管理能力，提供便捷的策略优化入口

**功能需求**：

1. **我的策略 - 查看详情**
   - 点击策略卡片后显示操作按钮
   - 点击"查看详情"弹出策略详情弹窗
   - 弹窗展示策略名称、描述、完整代码（代码高亮）

2. **我的策略 - 优化策略**
   - 策略卡片新增"优化策略"按钮
   - 点击后跳转到 AI 助手页面
   - 输入框上方显示"已附加策略"卡片
   - 用户可以添加优化需求描述后发送

3. **回测完成 - 优化策略**
   - 回测完成后在结果区域新增"优化策略"按钮
   - 点击后行为同上

**UI 设计**：

#### 我的策略卡片（点击后展开）
```
┌────────────────────────────────────┐
│  📈 RSI超买超卖策略        [×]    │
│  ─────────────────────────────────  │
│  RSI低于30买入，高于70卖出         │
│                                     │
│  收益率: +45.6%   胜率: 68%        │
│  ─────────────────────────────────  │
│  [📋 查看详情]  [🔧 优化策略]      │
│               [▶ 开始回测]         │
└────────────────────────────────────┘
```

#### 策略详情弹窗
```
┌───────────────────────────────────────────┐
│  RSI超买超卖策略                   [×]    │
├───────────────────────────────────────────┤
│  📝 策略描述                              │
│  基于 RSI 指标的超买超卖策略...           │
│                                           │
│  📊 回测指标                              │
│  收益率: +45.6%  胜率: 68%  回撤: 12.3%  │
│                                           │
│  💻 策略代码 (86 行)                      │
│  ┌───────────────────────────────────────┐│
│  │ from datetime import datetime         ││
│  │ from typing import Optional           ││
│  │ import pandas as pd                   ││
│  │ ...                                   ││
│  └───────────────────────────────────────┘│
│                                           │
│  [📋 复制代码]  [🔧 优化策略] [▶ 回测]   │
└───────────────────────────────────────────┘
```

#### AI 助手 - 附加策略卡片
```
┌───────────────────────────────────────────┐
│ 🤖 AI 助手                                │
├───────────────────────────────────────────┤
│ 👤: 帮我写一个 MACD 策略                  │
│ 🤖: 好的，这是一个 MACD 策略...           │
│                                           │
├───────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐   │
│ │ 📎 已附加策略                  [×]  │   │
│ │ RSI超买超卖策略 (86行代码)          │   │
│ │ 收益率: +45.6%  胜率: 68%           │   │
│ │ [👁 查看代码]                       │   │
│ └─────────────────────────────────────┘   │
│                                           │
│ ┌─────────────────────────────────────┐   │
│ │ 帮我优化这个策略，增加止损条件...    │   │
│ └─────────────────────────────────────┘   │
│                        [停止 ⏹] [发送 ➤]  │
└───────────────────────────────────────────┘
```

**技术方案**：

1. **策略详情弹窗**
   - 复用 `components/CodeBlock.tsx` 显示代码
   - 新增 `components/StrategyDetailModal.tsx`（或更新现有组件）

2. **附加策略功能**
   - `chatStore.ts` 新增 `attachedStrategy` 状态
   - 发送消息时，自动在用户消息前添加策略上下文
   - 附加策略卡片显示策略摘要（名称、行数、指标）

3. **优化策略流程**
   - 点击"优化策略"→ 设置 `attachedStrategy` → 跳转到 `/agent`
   - AI 助手页面检测到附加策略，显示附加卡片
   - 用户输入优化需求，发送时自动构建完整 prompt

**消息构建示例**：
```
用户实际输入：帮我增加一个止损条件

发送给后端的消息：
---
我想优化以下策略：

策略名称：RSI超买超卖策略
策略描述：基于 RSI 指标的超买超卖策略

```python
[完整策略代码]
```

我的优化需求：帮我增加一个止损条件
---
```

**页面/组件**：
| 文件 | 说明 |
|------|------|
| `components/StrategyDetailModal.tsx` | 新增/更新：策略详情弹窗 |
| `components/AttachedStrategyCard.tsx` | 新增：附加策略卡片 |
| `pages/Dashboard.tsx` | 更新：用户策略卡片操作按钮 |
| `pages/Agent.tsx` | 更新：支持附加策略显示 |
| `pages/backtest/BacktestStream.tsx` | 更新：新增"优化策略"按钮 |
| `stores/chatStore.ts` | 更新：新增 `attachedStrategy` 状态 |
| `api/strategy.ts` | 更新：获取策略详情 API |

**验收标准**：
- [x] 我的策略卡片点击后显示操作按钮
- [x] 点击"查看详情"弹出策略详情弹窗（含代码高亮）
- [x] 点击"优化策略"跳转 AI 助手，附加策略卡片显示
- [x] 附加策略卡片可查看代码、可移除
- [x] 发送消息时自动构建完整 prompt
- [x] 回测完成后显示"优化策略"按钮，功能同上

**交付物**：
- `components/StrategyDetailModal.tsx` ✅ - 策略详情弹窗
- `components/StrategyDetailModal.css` ✅ - 策略详情样式
- `components/AttachedStrategyCard.tsx` ✅ - 附加策略卡片
- `components/AttachedStrategyCard.css` ✅ - 附加策略样式
- `stores/chatStore.ts` ✅ - 新增 attachedStrategy 状态
- `pages/Dashboard.tsx` ✅ - 策略卡片操作按钮
- `pages/Agent.tsx` ✅ - 支持附加策略显示
- `pages/backtest/BacktestStream.tsx` ✅ - 优化策略按钮
- `components/Modal.tsx` ✅ - 新增 size 属性

---

### 阶段十四：工具调用卡片增强（回测 + AI 对话）⏳ 待开发

**目标**：在回测和 AI 对话界面，当 Agent 调用工具时，除展示工具调用卡片外，还需在卡片中用浅色小字滚动式展示当前正在调用的工具名

**现状**：
- 回测界面已有 `agent_tool` 消息卡片（显示 spinner + 工具名）
- AI 对话界面有 `action` SSE 事件（工具调用），但无独立卡片展示

**实现内容**：

1. **通用工具调用卡片组件** `components/ToolCallCard.tsx`
   - 滚动式浅色小字动画展示工具名
   - 支持 running / done 两种状态
   - running 时：spinner + 工具名从右到左滚动
   - done 时：checkmark + 工具名 + 结果摘要

2. **回测界面适配** `pages/backtest/BacktestStream.tsx`
   - 替换现有 agent_tool 卡片为通用组件
   - 添加工具名滚动动画

3. **AI 对话界面适配** `pages/Agent.tsx`
   - 消息列表中渲染 `action` 事件为工具调用卡片
   - 展示正在调用的工具名和参数摘要

**视觉设计**：
```
┌─────────────────────────────────────────┐
│  ⟳ 调用 get_recent_trades 工具中...    │  ← running 状态
│     ← 浅灰色小字，从右向左滚动          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  ✓ get_recent_trades                    │  ← done 状态
│     获取了最近 20 笔交易                │
└─────────────────────────────────────────┘
```

**涉及文件**：
| 文件 | 变更 |
|------|------|
| `components/ToolCallCard.tsx` | 新增：通用工具调用卡片 |
| `components/ToolCallCard.css` | 新增：滚动动画样式 |
| `pages/backtest/BacktestStream.tsx` | 更新：使用通用卡片组件 |
| `pages/backtest/BacktestStream.css` | 更新：移除旧卡片样式 |
| `pages/Agent.tsx` | 更新：渲染工具调用卡片 |
| `stores/chatStore.ts` | 可能更新：存储工具调用状态 |

**验收标准**：
- [ ] 回测界面工具调用卡片带滚动式工具名动画
- [ ] AI 对话界面工具调用时显示独立卡片
- [ ] running 状态有 spinner + 滚动文字
- [ ] done 状态有 checkmark + 结果
- [ ] 动画流畅不卡顿

---

## 更新日志

| 日期 | 版本 | 内容 |
|------|------|------|
| 2026-02-13 | v0.1 | 初始化项目文档 |
| 2026-02-13 | v0.2 | 完成阶段一~三 |
| 2026-02-13 | v0.3 | 简化技术栈：移除 Vite/Tailwind，改用 CRA + 原生 CSS |
| 2026-02-14 | v0.4 | 完成阶段四：流式回测与收藏功能 |
| 2026-02-14 | v0.5 | 完成阶段五：参数调整与策略保存 |
| 2026-02-14 | v0.6 | 完成阶段六：Agent 交互 |
| 2026-02-28 | v0.7 | 开始阶段七：收益率排行榜 |
| 2026-03-04 | v0.8 | 完成阶段八：策略预览与管理增强 |
| 2026-03-04 | v0.9 | 完成阶段九：Agent 对话界面 |
| 2026-03-06 | v1.0 | 完成阶段十二：AI 对话流式输出 |
| 2026-03-06 | v1.1 | 完成阶段十三：策略查看与优化入口 |
| 2026-03-13 | v1.2 | 新增阶段十四~十五：AgentScope ReAct Agent 前端适配规划（推理过程展示、Tool Groups 面板）|
| 2026-03-13 | v1.3 | 新增阶段十四规划：工具调用卡片增强（滚动式工具名展示） |